import 'package:test/test.dart';

void main() {
  group('getTodos() Test', () {
    test('정상적으로 데이터 가져오는지 확인', () {});
    test('올바른 Json 이 아닐 경우', () {});
    test('200이 아닌 값을 반환하는 경우', () {});
  });

  group('getTodo() Test', () {
    test('정상적으로 데이터 가져오는지 확인', () {});
    test('ID 가 없는 경우', () {});
    test('올바른 Json 이 아닐 경우', () {});
    test('200이 아닌 값을 반환하는 경우', () {});
  });
}
