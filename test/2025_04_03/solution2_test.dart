import 'package:test/test.dart';

void main() {
  group('saveImages() Test', () {
    test('fetchImage 잘 되는지 확인', () {});
    test('saveImage 잘 되는지 확인', () {});
    test('fetchImage, saveImage 실패', () {});
  });

  group('saveImage() Test', () {
    test('fetchImage 잘 되는지 확인', () {});
    test('saveImage 잘 되는지 확인', () {});
    test('fetchImage, saveImage 실패', () {});
  });
}
